# Hiring Analytics Lakehouse

## Identity and scope

- Status: in progress
- User outcome: Admin can query bounded, aggregate hiring analytics produced from local Kafka events without coupling request handling to Spark.
- Authorized scope: a local Scala 2.13 Spark/Delta analytics module, Compose integration, Bronze/Silver/Gold datasets, an Admin GraphQL read model, account-deletion erasure requests, tests, and canonical documentation.
- Non-goals: cloud deployment, Structured Streaming, a dashboard, current-state enrichment, changes to the operational event envelope, or a 365-day historical analytics store.
- Security objective: remove delayed outbox replay as a source of deleted-subject analytics data. The permanent scrubbed `Deleted` account record is the replay-deny source; HMAC deletion markers cover bounded Kafka and analytical retention and expire 31 days after erasure completion. Security review remains pending until this behavior is implemented and independently verified.
- Release status: local development evidence is in scope. Do not describe K=10 suppression as a formal anonymity guarantee.

## Source facts and decisions

- `hiring.operational-events` is the current unversioned seven-field source envelope. Spark consumes that exact shape with declarative parsing and does not reimplement the Scala Circe decoder.
- Required `eventId` and `aggregateId` values must be nonblank after trimming. Null-valued Kafka tombstones are malformed and use topic/partition/offset coordinates as a stable quarantine identity across replayed ranges.
- GraphQL mutations run through `MutationReceiptRepository`, which supplies a receipt-owned `MutationWriteContext`; the current account deletion joins it to tombstone the account, close open recruiter jobs, and append `JOB_CLOSED` outbox events atomically.
- `deleteMyAccount` requires a non-noop context for the analytics-aware path. A new erasure-request port joins the same session before account deletion; direct repository deletion remains a non-analytics repository operation.
- Deleted account tombstones retain the account UUID and `Deleted` status after personal profile/authentication fields are scrubbed. Outbox publication must check all persisted subject references against this durable deny source on every attempt.
- Outbox records currently persist actor ID and opaque payload but no indexed subject-reference list. Add actor and every candidate ID as internal `subjectIds`; this does not change the seven-field Kafka envelope. A versioned migration must backfill and validate every retained row before publisher startup. Any undecodable or incomplete row blocks startup; it must be manually resolved or terminally quarantined without publication, never guessed.
- Outbox sends and account deletion share a per-subject publication fence. A publisher obtains a durable, expiring lease for every subject in a record, rechecks tombstones while holding that lease, and retains it until Kafka send completion/ack or definitive failure. Deletion first installs its tombstone and erasure request transactionally, then waits for all earlier subject leases to finish/expire and drains the broker's committed offsets before erasure can complete. Publishers cannot acquire a new lease after the tombstone exists. Kafka ACLs restrict writes to this publisher identity. An erasure remains active until the drain point is beyond all pre-delete sends; a crash recovers via lease expiry and repeated drain.
- Analytics report state and generation are stored independently of the expiring report payload. Account deletion atomically increments the durable generation and hides the current snapshot. Each batch captures the generation before processing; publication atomically compares the expected generation and requires a strictly newer run revision before replacing the snapshot. A missing snapshot never resets the generation. A failed comparison leaves the newer hidden/visible state untouched.
- Bronze and quarantine retain raw records for seven days. Silver, Gold, and published Mongo snapshots retain 30 days. Gold is rebuilt only from retained Silver data.
- Silver pseudonymizes only allowed identifier fields with a dedicated HMAC secret. Gold exposes only groups with at least ten contributing subjects/events.
- The runnable batch reads up to 100,000 `Pending` requests from `analytics_erasure_requests`, validates their UUID subject IDs, and HMAC-tokenizes them before any run mutation. A larger backlog fails before Delta mutation. When markers exist, it expires Silver, removes matching stored rows, and rebuilds local Gold before it reads the requested Kafka range. It does not publish or hide the GraphQL Mongo snapshot, purge raw Bronze/quarantine/outbox data, coordinate publisher drain, complete or expire erasure requests, or prevent a concurrent deletion race.

## Metric and publication decisions

- Event-time days use UTC calendar boundaries. Funnel and skill activity use `occurredAt`. GraphQL period bounds select UTC calendar-day buckets inclusively and are limited to 30 calendar days. `asOf` is the completion time of the latest successfully published run; the batch is manually invoked and has no freshness SLO.
- A time-to-hire observation uses the first `APPLICATION_CREATED` time and first subsequent `Hired` status time for an application. Missing creation/hire events, a hire before creation, or conflicting lifecycle facts are excluded and counted only when the remaining published count passes suppression.
- Suppression is evaluated independently for every output cell, using distinct pseudonymized subjects as the contributor count. Funnel values count distinct applications, skill activity counts posting events, and time-to-hire summarizes eligible applications; no subject may contribute more than once to a metric cell's suppression count. Every exposed count, including eligible/excluded counts, must satisfy suppression or be omitted.
- Any malformed or conflicting event in the requested offset range is quarantined and marks the run `QUALITY_BLOCKED`. Valid facts may be retained in Silver for later deterministic rebuilds, but neither Gold nor the Mongo snapshot may advance from a quality-blocked run. Existing published snapshots remain available until a later eligible run succeeds.
- The runner continues to consume the existing seven-field operational event envelope. Unknown event types or aggregate types are quarantined as malformed for this slice; envelope evolution remains a separate compatibility change.
- The batch erasure workflow is resumable and idempotent. Pending requests remain active markers until the publisher fence/drain, purge, Gold rebuild, and guarded snapshot publish succeed; then the marker is retained for 31 days. A later account deletion changes the durable snapshot generation and prevents an older batch from publishing.
- Kafka is immutable within its retention window. Delta Bronze/quarantine rows must carry subject references independently of successful parsing. When an input cannot be attributed, erasure purges the entire affected raw/quarantine range (or the complete raw/quarantine dataset if precise range attribution is unavailable), then verifies no readable Delta snapshot/file can reconstruct it. Erasure cannot complete while affected retained Kafka records can still be replayed; the bounded source retention and publisher drain are part of that proof.

## Run and recovery contract

- A run is identified by an immutable run ID and exact topic/partition/offset ranges. Reusing a run ID with different ranges is rejected. Exact range retries are idempotent; overlapping and concurrent runs against the same lakehouse are serialized or rejected. Manifest states distinguish `STARTED`, `QUALITY_BLOCKED`, `FAILED`, and `PUBLISHED`.
- A run is not marked `PUBLISHED` until all Bronze, Silver, quarantine, Gold, manifest, and Mongo snapshot writes have succeeded and reconciliation has passed. Failure at any write boundary leaves the prior published snapshot readable; retrying the same run converges without duplicate facts. Mongo publication is an atomic replacement of one complete snapshot, and older runs cannot overwrite a newer published `asOf`.
- Reconciliation records requested, read, deduplicated, valid, quarantined, conflicting, suppressed, and published counts per range. Empty reads are distinguished from expired or unavailable offsets; incomplete requested ranges fail rather than silently producing an empty report.
- Kafka retains source records for seven days. If requested offsets have expired and no retained local Bronze copy can satisfy the run, the operator receives a clear unrecoverable-range result and the report is not advanced. Recovery is a new explicit range/run, not implicit offset discovery.
- Retention means expired rows are unavailable to all reads and report rebuilds, and expired data files are eventually physically reclaimed under a documented Delta-safe maintenance procedure. Mongo snapshot expiry remains enforced independently.
- HMAC marker records identify the key version used. The reader accepts active and retiring keys; rotation dual-computes tokens for all accepted key versions, and key retirement is blocked until every Delta row, report, and marker derived from that key has expired or been safely re-keyed. Secrets remain outside source control, logs, run manifests, and Compose defaults.
- The opt-in local workload is a deterministic 100,000-record synthetic range over one topic partition and a 30-day event-time window, with a fixed seed and a documented mix of lifecycle, job-created, malformed, duplicate, and conflicting records. Record the exact generated counts and the host/JDK/Spark/Delta settings with each run. This is a reproducibility baseline, not a latency or throughput SLO; no cost claim is made for the local-only run.

## Acceptance and evidence

| ID | Given / When / Then | Evidence | Actual outcome |
|---|---|---|---|
| HAL-01 | A bounded Kafka range is run twice | Bronze deduplicates by topic/partition/offset; Silver deduplicates by event ID; manifests reconcile counts | Bounded Kafka/Delta runner and transformation tests are implemented; repeat-run integration evidence is pending |
| HAL-02 | Lifecycle, duplicate-hire, malformed, or conflicting events arrive | Canonical lifecycle facts aggregate; invalid/conflicting input quarantines and prevents publish; blank required IDs are malformed; tombstone quarantine identity is stable across replay | Transformation tests cover blank identifiers, null Kafka tombstone replay, malformed/conflicting input, and duplicate-hire exclusion; Gold-to-Mongo publish wiring is not implemented |
| HAL-03 | An active Admin queries published analytics | Only a stored active Admin can read the latest complete snapshot; the 30-calendar-day UTC period is bounded, `asOf` is visible, and missing/expired/unavailable snapshots return sanitized failures | `AnalyticsReportingServiceSpec`: 3 passed; expand forbidden-role, period-boundary, and stale-snapshot cases |
| HAL-04 | A GraphQL account deletion succeeds or rolls back | Receipt, erasure request, account tombstone, closed jobs, and close-event outbox rows are atomic | `UserAccountServiceSpec`: 15 passed; Mongo transaction integration remains unverified |
| HAL-05 | A deleted subject's event is pending, failed, claimed, manually re-queued, or in-flight | Versioned subject-reference migration gates publisher startup; every send holds a subject lease and checks tombstones; deletion fences and drains earlier sends; Kafka ACLs prevent bypass; tombstone permanently denies retries; matching outbox data is purged | Not implemented; migration gate, subject leases, tombstone checks, ACL contract, drain recovery, and race tests required |
| HAL-06 | Retention passes or an HMAC key is rotated | Bronze/quarantine expire at seven days; Silver/Gold/snapshots and markers expire at 30/31 days; expired Delta data is physically reclaimed safely; active markers remain matchable across key rotation | Batch expiry is implemented for Bronze, quarantine, and Silver. Gold/snapshot/marker enforcement, physical reclamation, and rotation behavior remain unimplemented |
| HAL-07 | The fixed local deterministic workload executes | Exact generated counts, physical plan, shuffle/file counts, duration, memory, storage, environment, and reconciliation are reported | Opt-in local Compose batch deployment exists; harness and measurements are pending |
| HAL-08 | An account erasure request is pending or marker storage is unavailable | The batch loads active markers before any manifest/data mutation, fails closed if unavailable, waits for publisher fence/drain, purges matching outbox and attributed Delta rows (whole affected raw scope if attribution fails), verifies physical reclamation/rebuild safety, rebuilds Gold, guarded-publishes, and completes only after Kafka replay is impossible within retention | Partially implemented: runnable batch reads and validates pending Mongo requests before mutation, purges matching Silver rows, and rebuilds local Gold before Kafka access, including when the later range quality-blocks. Remaining: publisher fence/drain, outbox and raw Bronze/quarantine purge, physical reclamation, marker completion/expiry, generation-guarded Mongo snapshot hide/publish, race/recovery integration evidence |
| HAL-09 | A metric cell has fewer than ten distinct contributing subjects | No affected cell or small eligible/excluded count is exposed; each metric applies its contributor rule independently. Review fixed-period results across successive snapshots for differencing risk; K=10 is not represented as formal anonymity | Funnel status cells, time-to-hire eligible/excluded counts, and skill activity now threshold distinct HMAC subjects; tests cover one-subject repeated applications/postings and small status/excluded cells. Differencing review remains open |
| HAL-10 | A range contains malformed/conflicting input or a write fails mid-run | Bad input prevents snapshot publication; retries of the same exact range converge; partial/stale/concurrent runs cannot replace a newer complete snapshot | Marker tokens are cached/materialized before Delta writes; malformed/conflicting input returns explicit `QualityBlocked`, records `QUALITY_BLOCKED`, and does not advance Gold. Valid runs report `Published` and terminal manifest `PUBLISHED`. Exact-range identity/retry, Mongo publish barrier/CAS, and injected mid-write recovery remain unimplemented |
| HAL-11 | Requested offsets are expired, missing, or the requested range is empty | The run distinguishes these cases, reports exact reconciliation counts, and never marks an incomplete range complete or advances the report | Not implemented; bounded batch source and manifest tests required |
| HAL-12 | Deletion begins during an analytics run or publisher/batch crashes during erasure | A durable generation independent of snapshot expiry is incremented transactionally; compare-and-set is atomic with snapshot replacement and requires a newer revision; a stale run cannot restore data; leases/drain and marker retention survive crashes; rerun completes pending erasure idempotently | Not implemented; replica-set generation race, lease expiry/drain, and injected crash/restart integration tests required |

## Checkpoint and review

- Current verified subset: root `sbt test` passed 350 tests on Java 17; analytics `Test / test` passed 19 tests, and `IntegrationTest / testOnly com.example.hiring.analytics.MongoActiveDeletionMarkerIntegrationSpec` passed 4 disposable-Mongo tests. Analytics formatting checks passed. Compose syntax was validated with `docker compose config --quiet`; no full Compose/Kafka source-to-projection, Mongo snapshot publication, migration, physical retention, or workload evidence has been collected.
- Initial architecture and security plan reviews returned FAIL because the in-flight publication fence, migration gate, and generation compare-and-set contracts were underspecified. Those requirements are now explicit. The current scoped marker-source change received Code Reviewer PASS, Security Engineer PASS, and QA PASS; these verdicts do not certify the remaining end-to-end erasure controls.
- Full Phase 6 gates remain open: a deleted subject must be shown unable to re-enter through outbox repair, an in-flight send, Kafka replay, unattributed Bronze/quarantine, Delta rebuild, alternate Kafka producers, key rotation, or stale snapshot publication. Mongo/replica-set, Compose, live Kafka, physical retention, and workload evidence is still required.
- Next implementation order: implement and verify versioned outbox subject-reference migration plus publisher fencing/deny/drain (HAL-05); complete the remaining HAL-08 work: resumable outbox/raw Delta purge, physical reclamation, marker completion/expiry, and generation-guarded report hide/publish; implement HAL-12 concurrency/crash recovery; complete exact-range identity/retry, failure recovery, offset-expiry handling, retention, and key-rotation behavior (HAL-06, HAL-10, HAL-11); then collect deterministic workload evidence (HAL-07).
