type Health {
  status: HealthStatus!
}

enum HealthStatus {
  UP
}

type Query {
  health: Health!
  readiness: Readiness!
}

type Readiness {
  status: ReadinessStatus!
}

enum ReadinessStatus {
  READY
  NOT_READY
}
